# Planeja+ — Frontend

Repositório do frontend React do **Planeja+**.

O app está em `planeja-app/`. Para instalar e rodar:

```bash
cd planeja-app
npm install
cp .env.example .env
npm start
```

Consulte `planeja-app/README.md` para documentação completa.
